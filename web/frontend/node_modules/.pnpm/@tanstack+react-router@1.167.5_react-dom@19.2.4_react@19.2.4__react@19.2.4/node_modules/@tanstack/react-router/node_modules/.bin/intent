#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")
basedir_win="$basedir"
exe=""
msys=""

case `uname -a` in
  *CYGWIN*|*MINGW*|*MSYS*)
    if command -v cygpath > /dev/null 2>&1; then
      basedir_win=`cygpath -w "$basedir"`
    fi
    exe=".exe"
    msys="true"
  ;;
  *WSL2*)
    if command -v wslpath > /dev/null 2>&1; then
      basedir_win="$(wslpath -w "$basedir" 2> /dev/null)"
      if [ $? -ne 0 ] || [ -z "$basedir_win" ]; then
        basedir_win="$basedir"
      else
        exe=".exe"
      fi
    fi
  ;;
esac

if [ -n "$exe" ] && [ -x "$basedir/node.exe" ]; then
  exec "$basedir/node.exe"  "$basedir_win/../../../router-core/bin/intent.js" "$@"
elif [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../router-core/bin/intent.js" "$@"
elif command -v node >/dev/null 2>&1; then
  exec node  "$basedir/../../../router-core/bin/intent.js" "$@"
elif [ -n "$exe" ] && command -v node.exe >/dev/null 2>&1; then
  exec node.exe  "$basedir_win/../../../router-core/bin/intent.js" "$@"
else
  exec node  "$basedir/../../../router-core/bin/intent.js" "$@"
fi
# cmd-shim-target=/Users/horatiubudai/ceo/ShrimpAI/jameclaw-main/jameclaw/Jameclaw/web/frontend/node_modules/.pnpm/@tanstack+react-router@1.167.5_react-dom@19.2.4_react@19.2.4__react@19.2.4/node_modules/@tanstack/router-core/bin/intent.js
